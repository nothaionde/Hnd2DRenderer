package hnd.src.events;

/**
 * An enumeration of all possible types of events that can occur in the system.
 */
public enum EventType {
	/**
	 * Represents an event in which a key is pressed on the keyboard.
	 */
    KeyPressedEvent
}
